package com.example.util.simpletimetracker.domain.recordAction.model

enum class RecordQuickAction {
    CONTINUE,
    REPEAT,
    DUPLICATE,
    MOVE,
    MERGE,
    SPLIT,
    ADJUST,
    SHORTCUT,
    STOP,
    MULTISELECT,
    CHANGE_ACTIVITY,
    CHANGE_TAG,
}